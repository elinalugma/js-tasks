let  newArray = [];
function capital(array){
  for(let i = 0; i < array.length; i++){
    let compare = array[i][0];
    if (array[i][0]=== array[i][0].toUpperCase()){
    newArray = array[i];
     console.log(newArray);
    }
  }
}
